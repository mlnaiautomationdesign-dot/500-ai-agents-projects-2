# AI Influencer Factory — Python Dependencies

# ── Core ──────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# ── LLM / AI APIs ─────────────────────────────────────────────────────────────
openai>=1.12.0           # GPT-4, DALL-E 3, TTS-1
anthropic>=0.18.0        # Claude API (alternative LLM)

# ── Text-to-Speech ────────────────────────────────────────────────────────────
TTS>=0.22.0              # Coqui TTS — includes XTTS-v2 (local voice cloning)
gTTS>=2.5.0              # Google TTS fallback (no GPU required)

# ── Social Media APIs ─────────────────────────────────────────────────────────
tweepy>=4.14.0           # Twitter/X API v2

# ── Media Processing ──────────────────────────────────────────────────────────
Pillow>=10.2.0           # Image manipulation
moviepy>=1.0.3           # Video composition (body merge, basic render)
# ffmpeg-python>=0.2.0   # ffmpeg Python bindings (optional, ffmpeg must be installed)

# ── Scheduling ────────────────────────────────────────────────────────────────
apscheduler>=3.10.0      # Lightweight task scheduling

# ── Development & Quality ─────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
black>=24.1.0
flake8>=7.0.0
mypy>=1.8.0

# ── Notes ─────────────────────────────────────────────────────────────────────
# SadTalker (face animation): clone to models/SadTalker — not on PyPI
# RIFE (60fps upscaling): clone to models/RIFE — not on PyPI
# ffmpeg: install system-level (apt install ffmpeg / brew install ffmpeg)
